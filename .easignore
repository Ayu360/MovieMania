node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Native projects generated by `expo prebuild` — regenerated from app.json + plugins/
ios/
android/

# Firebase iOS config (only exclude the iOS one — Android's google-services.json
# is untracked-but-required for the build to succeed)
GoogleService-Info.plist

# Local env source files (only .env is read at bundle time — the .env.dev/.env.prod
# are just copy sources for yarn env:dev / yarn env:prod)
.env.local
.env.dev
.env.prod

# Claude Code local session state
.claude/settings.local.json
.claude/*.lock
